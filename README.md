# ha-tesla-fleet-mcp

Home Assistant OS add-on wrapping [tesla-fleet-mcp](https://github.com/supervised-nl/tesla-fleet-mcp) as always-on Streamable HTTP.

Expose via Cloudflared public hostname to `http://local_tesla_fleet_mcp:8787` (container DNS). Do not publish host ports.
